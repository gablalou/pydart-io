---
phase: 02-full-dtype-structural-coverage
plan: 05
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - crates/flint-python/src/pandas.rs
  - tests/python/test_multi_chunk_diagnostics.py
autonomous: true
requirements: [CONV-08]
must_haves:
  truths:
    - "D-12: a multi-chunk Arrow-backed pandas column (e.g. pd.concat of two int64[pyarrow] frames -> a 2-chunk ChunkedArray) still round-trips ALL rows through from_pandas -> to_pandas (the arrow::compute::concat fallback is retained, an honest copy, not zero-copy) -- the CR-01 correctness fix is not regressed."
    - "D-13: plan_column/ColumnConversionRecord is now chunk-count-aware -- a multi-chunk column's ColumnConversionRecord is corrected to zero_copy=false with a reason attributing the copy to multi-chunk concatenation (structural copy), so copy_report() honestly reflects the concat copy that previously showed as zero_copy=true (closes the DIAG-01/DIAG-02 override from 01-VERIFICATION.md)."
    - "D-14: from_pandas(df, strict=True) now RAISES flint.ZeroCopyRequiredError for a multi-chunk Arrow-backed column that previously succeeded silently -- with no opt-in flag (e.g. allow_rechunk) to bypass this under strict mode. This is the correct DIAG-01 contract behavior even though it changes prior observed behavior for existing callers."
    - "A single-chunk Arrow-backed column is unaffected: it still reports zero_copy=true and still succeeds under strict=True (the chunk-count correction only fires when batches.len() > 1)."
    - "FLAGGED ASSUMPTION (edge-probe unresolved, CONV-08 unclassified): CONV-08 edge behavior is taken from CONTEXT.md D-12/D-13/D-14 and 01-VERIFICATION.md's DIAG-01/02 override as the authoritative edge spec (concat-to-single-batch retained; diagnostics made chunk-count-aware; strict now rejects multi-chunk). Surfaced, not silently dropped."
  prohibitions:
    - "MUST NOT report a multi-chunk column's concat as zero-copy (D-13) -- copy_report and strict mode must honestly reflect that the concat is a real copy; the whole point of this slice is closing the DIAG-01/02 honesty gap."
    - "MUST NOT add an opt-in flag (e.g. allow_rechunk) that lets strict=True silently accept a multi-chunk column (D-14) -- strict mode must raise; there is no bypass."
    - "MUST NOT query pandas' private num_chunks attribute (series.array._pa_array.num_chunks) to pre-empt the chunk count (Pitfall 6 Strategy A) -- use Strategy B (post-hoc correction from the observed batches.len()), so Flint does not depend on a private, version-unstable pandas API."
    - "MUST NOT re-derive the copy-vs-borrow decision in a second place (RESEARCH Pitfall 2) -- the chunk-count correction refines the single ColumnConversionRecord after the observed batch count, it is not a parallel decision matrix; plan_column's pure-Rust input/output shape stays unchanged."
  artifacts:
    - crates/flint-python/src/pandas.rs
    - tests/python/test_multi_chunk_diagnostics.py
  key_links:
    - "import_column_via_pandas_stream changes return type from PyResult<ArrayRef> to PyResult<(ArrayRef, usize)> (array + observed batches.len()); from_pandas uses that count to correct the already-computed ColumnConversionRecord (zero_copy=false + multi-chunk reason) when count > 1, BEFORE check_strict/build_copy_report consume the records. diagnostics.rs is unchanged (verify no-op)."
---

<assumption_delta_decision>
noun: "chunked/multi-batch column representation"
decision: "no-change"
rationale: "Per D-12, per-column independent chunk-boundary alignment is deferred; arrow::compute::concat-to-single-batch is kept as the primary internal representation (not promoted to genuine per-column chunk-boundary zero-copy, and not added alongside). D-13 adds chunk-count-awareness only to diagnostics (the ColumnConversionRecord produced in from_pandas), not to the storage representation itself. The Assumption-Delta detector fired on Success Criterion 5's 'multiple chunks' pluralization; discuss-phase already resolved it via D-12 (this is that recorded outcome, not a new open question)."
</assumption_delta_decision>

<objective>
Deliver CONV-08 (multi-chunk) as the final vertical slice, closing the DIAG-01/DIAG-02 honesty gap
carried forward from 01-VERIFICATION.md.

Behavior delivered:
- Multi-chunk Arrow-backed columns still round-trip all rows (D-12, CR-01 not regressed).
- copy_report() now honestly reports a multi-chunk column as zero_copy=false with a multi-chunk
  reason (D-13) -- the concat copy that previously showed as zero_copy=true is now truthful.
- strict=True now raises flint.ZeroCopyRequiredError for a multi-chunk column, with no bypass flag
  (D-14) -- the correct DIAG-01 contract behavior, even though it changes prior observed behavior.

Implementation is Strategy B (RESEARCH Pitfall 6, recommended): import_column_via_pandas_stream
returns the observed batch count alongside the array, and from_pandas corrects the already-computed
ColumnConversionRecord after the fact when the count exceeds one. This avoids pandas private APIs
and keeps plan_column's pure matrix unchanged (the correction is a refinement of the single source
of truth, not a second decision-maker). diagnostics.rs needs no change (verify no-op).

Purpose: make the diagnostics honest about the copy the pipeline already performs -- the last piece
of the phase's zero-copy-honesty commitment.
Output: import_column_via_pandas_stream signature change, a from_pandas post-hoc correction step,
and a multi-chunk diagnostics test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-dtype-structural-coverage/02-CONTEXT.md
@.planning/phases/02-full-dtype-structural-coverage/02-RESEARCH.md
@.planning/phases/02-full-dtype-structural-coverage/02-PATTERNS.md
@.planning/phases/01-core-zero-copy-round-trip-interop/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: import_column_via_pandas_stream returns (ArrayRef, usize); from_pandas corrects the ColumnConversionRecord post-hoc (Strategy B)</name>
  <files>crates/flint-python/src/pandas.rs</files>
  <read_first>
    - crates/flint-python/src/pandas.rs (import_column_via_pandas_stream lines 192-219 -- return type + single/multi-batch branches; from_pandas per-column loop lines 127-168 -- where records are pushed and arrays selected; ColumnConversionRecord struct lines 46-71)
    - crates/flint-python/src/diagnostics.rs (check_strict + build_copy_report -- confirm they only read record.zero_copy/record.reason, so no change needed here)
    - .planning/phases/02-full-dtype-structural-coverage/02-RESEARCH.md (Pitfall 6 Strategy B full text)
    - .planning/phases/02-full-dtype-structural-coverage/02-PATTERNS.md (pandas.rs "Fallback conversion mechanism -- DO NOT MODIFY" except the return signature)
    - .planning/phases/01-core-zero-copy-round-trip-interop/01-VERIFICATION.md (DIAG-01/DIAG-02 override + root-cause: plan is computed before the stream is read)
  </read_first>
  <action>
    Change import_column_via_pandas_stream's return type from PyResult<ArrayRef> to
    PyResult<(ArrayRef, usize)>, returning the observed batches.len() alongside the array. The
    single-batch branch returns (array, 1); the multi-batch concat branch returns (concatenated,
    batches.len()). Do NOT change the internal concat/single-batch logic itself (it is correct per
    CR-01) -- only surface the count.
    In from_pandas's per-column loop, when a column's array came from
    import_column_via_pandas_stream and the returned count > 1, correct that column's
    ColumnConversionRecord: set zero_copy=false and set reason to a multi-chunk-specific,
    free-text sentence (e.g. that the column arrived as N Arrow chunks and was concatenated into one
    contiguous buffer via arrow::compute::concat, which is a copy). The correction must happen
    AFTER the record is pushed (or adjust the push) but BEFORE from_pandas returns, so
    check_strict/build_copy_report see the corrected records. Keep the reason a free-text String
    (Claude's-Discretion item: no structured reason-category enum needed; vary the sentence,
    consistent with existing plan_column reasons). Do NOT modify plan_column's signature (no
    chunk_count parameter). Do NOT modify diagnostics.rs. The borrow_numpy_numeric_column path
    (which does not go through the stream) is inherently single-chunk and needs no correction.
    Note the counterpart branch in from_pandas that already destructures the array from the
    stream-import call site must now destructure (array, count).
  </action>
  <verify>
    <automated>cd /home/pc_gab_c/dev/test-project-sample && uv run maturin develop && uv run pytest tests/python -q</automated>
  </verify>
  <acceptance_criteria>
    - Extension compiles; full existing pytest suite stays green EXCEPT any pre-existing test that
      asserted multi-chunk-under-strict silent success (see Task 2 -- none is expected, but if one
      exists it must be updated there, not left failing).
    - import_column_via_pandas_stream returns (ArrayRef, usize) (source assertion); single-batch
      returns count 1, multi-batch returns the true batch count.
    - from_pandas corrects a multi-chunk column's ColumnConversionRecord to zero_copy=false with a
      multi-chunk reason before returning (source assertion).
    - diagnostics.rs is unchanged (git diff shows no modification to that file).
  </acceptance_criteria>
  <done>Strategy B implemented: chunk count observed, ColumnConversionRecord corrected post-hoc; diagnostics.rs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Audit existing tests for the D-14 behavior change, then add multi-chunk diagnostics tests</name>
  <files>tests/python/test_multi_chunk_diagnostics.py, tests/python/test_strict_mode.py</files>
  <read_first>
    - tests/python/test_strict_mode.py (existing strict-mode assertions -- audit for any multi-chunk-under-strict expectation)
    - tests/python/test_round_trip.py (test_from_pandas_preserves_all_rows_of_multi_chunk_arrow_backed_column lines 99-113 -- the CR-01 multi-chunk fixture; note it uses default strict, so it stays valid)
    - tests/python/test_copy_report.py (copy_report per-column status assertion style)
    - .planning/phases/01-core-zero-copy-round-trip-interop/01-VERIFICATION.md (DIAG-01/02 override -- the exact gap this closes)
  </read_first>
  <action>
    First, audit tests/python (grep for multi-chunk / pd.concat + strict) for any existing assertion
    that expects a multi-chunk column to SUCCEED under strict=True. If one exists, update it to the
    new D-14 contract (strict now raises) and note the change in the SUMMARY. Expectation from the
    current tree: the CR-01 test uses default strict (not strict=True) and test_strict_mode.py has
    no multi-chunk case, so no existing test should need updating -- but confirm by inspection, do
    not assume.
    Then create tests/python/test_multi_chunk_diagnostics.py with:
    - test_multi_chunk_column_still_round_trips_all_rows: rebuild the pd.concat 2-chunk
      int64[pyarrow] fixture; from_pandas (default) -> to_pandas; assert all 6 rows survive (D-12,
      CR-01 not regressed).
    - test_multi_chunk_column_reported_as_copy_in_copy_report: same fixture; assert copy_report()'s
      status for that column is zero_copy=False with a reason mentioning chunk/concat (D-13).
    - test_single_chunk_arrow_column_still_zero_copy: a single-chunk int64[pyarrow] column; assert
      copy_report zero_copy=True and reason None (correction only fires for >1 chunk).
    - test_strict_mode_now_rejects_multi_chunk_column: the 2-chunk fixture with strict=True; assert
      flint.Table.from_pandas raises flint.ZeroCopyRequiredError naming the column (D-14 behavior
      change).
    - test_single_chunk_arrow_column_still_succeeds_under_strict: a single-chunk int64[pyarrow]
      column with strict=True; assert it does NOT raise (proves the change is scoped to multi-chunk).
    - test_copy_report_and_strict_agree_for_multi_chunk (single-source-of-truth): assert the
      multi-chunk column that copy_report marks zero_copy=False is exactly the column strict mode
      raises on (mirrors the existing test_copy_report_agrees_with_strict_mode_rejection_per_column
      pattern).
  </action>
  <verify>
    <automated>cd /home/pc_gab_c/dev/test-project-sample && uv run pytest tests/python/test_multi_chunk_diagnostics.py tests/python/test_strict_mode.py tests/python/test_copy_report.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All test_multi_chunk_diagnostics.py tests pass; test_strict_mode.py and test_copy_report.py
      stay green.
    - The D-14 test asserts strict=True raises flint.ZeroCopyRequiredError for the multi-chunk column.
    - The single-chunk-under-strict test asserts NO raise (change scoped to multi-chunk).
    - copy_report/strict agreement test confirms the same column drives both (single source of truth).
    - SUMMARY records whether any existing test needed updating for the D-14 change (expected: none).
  </acceptance_criteria>
  <done>D-14 behavior change verified and audited; multi-chunk diagnostics honesty proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multi-chunk Arrow column -> diagnostics report | The pipeline performs a concat copy for multi-chunk columns; the integrity concern is whether that copy is honestly reported to the caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering / Repudiation | copy_report / strict-mode diagnostics for multi-chunk columns | medium | mitigate | D-13 chunk-count-awareness corrects the ColumnConversionRecord to zero_copy=false so the diagnostics cannot silently misrepresent a real concat copy as zero-copy; D-14 makes strict mode raise rather than silently accept -- upholding the project's zero-copy-honesty contract (RESEARCH State of the Art / 01-VERIFICATION.md DIAG-01/02). |
| T-02-SC | Tampering | cargo/pip package installs | high | accept | N/A this phase -- no new packages introduced (RESEARCH Package Legitimacy Audit). No install tasks. |
</threat_model>

<verification>
- `uv run maturin develop && uv run pytest tests/python -q` all green (with the D-14 change accounted for).
- diagnostics.rs unchanged (git diff clean for that file).
- Multi-chunk columns: all rows preserved (D-12), reported as copy (D-13), rejected under strict (D-14); single-chunk unaffected.
</verification>

<success_criteria>
- Multi-chunk columns round-trip all rows and are honestly reported as copies (CONV-08 / D-12 / D-13).
- strict=True rejects multi-chunk columns with no bypass flag; single-chunk unaffected (D-14).
- plan_column matrix and diagnostics.rs unchanged (single-source-of-truth preserved).
</success_criteria>

## Artifacts this phase produces (Plan 05 contributions)
- `import_column_via_pandas_stream` signature change to `PyResult<(ArrayRef, usize)>` (crates/flint-python/src/pandas.rs).
- from_pandas post-hoc ColumnConversionRecord correction for multi-chunk columns (Strategy B).
- `tests/python/test_multi_chunk_diagnostics.py`: new Python test file for CONV-08.
- No change to plan_column, diagnostics.rs, or FlintError (single-source-of-truth + reuse).

<output>
Create `.planning/phases/02-full-dtype-structural-coverage/02-05-SUMMARY.md` when done.
</output>
