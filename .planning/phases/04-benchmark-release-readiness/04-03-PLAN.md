---
phase: 04-benchmark-release-readiness
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .github/workflows/wheels.yml
  - .github/workflows/ci.yml
  - .github/workflows/compat-matrix.yml
autonomous: false
requirements: [PKG-01, PKG-02, PKG-03]
user_setup:
  - service: github
    why: "GitHub Actions workflows (PKG-01 wheel matrix, PKG-02 compat matrix) cannot execute without the repo hosted on GitHub. This repo currently has no git remote configured."
    dashboard_config:
      - task: "Create the GitHub repo and push. Make it PUBLIC so the free native `ubuntu-24.04-arm` hosted runner (Assumption A3, unverified in research) is available for the aarch64 Linux wheel build; if the repo must be private, the aarch64 job needs a QEMU cross-compile fallback (docker/setup-qemu-action) instead."
        location: "GitHub (create repo, git remote add origin, git push)"

must_haves:
  truths:
    - "A wheel builds locally for the host platform via `maturin build --release` and installs cleanly into a fresh venv via `uv pip install`, and `import pydart` succeeds from that install (PKG-01, PKG-03)."
    - "wheels.yml declares every D-34 (OS, arch) cell — linux x86_64, linux aarch64, macOS x86_64, macOS arm64, windows x86_64 — with none silently omitted (PKG-01)."
    - "compat-matrix.yml declares both endpoints: oldest {Python 3.11, numpy 2.3.0, pandas 3.0.0} and newest {Python 3.12, numpy 2.5.1, pandas 3.0.5} (PKG-02, D-37)."
    - "ci.yml runs cargo test + maturin develop + pytest on push/PR (mirrors the project's configured build/test commands)."
  artifacts:
    - ".github/workflows/wheels.yml (maturin-action build matrix across the five D-34 cells, artifact upload)"
    - ".github/workflows/ci.yml (cargo test + maturin develop + pytest)"
    - ".github/workflows/compat-matrix.yml (numpy/pandas oldest+newest endpoints on Python 3.11 and 3.12)"
  key_links:
    - "the wheel-build job builds against the numpy floor, not the newest numpy (numpy ABI is forward-compatible only — Pitfall 3)"
    - "the aarch64 Linux job uses `ubuntu-24.04-arm` (native runner) when the repo is public, avoiding QEMU"
  prohibitions:
    - statement: "The shipped wheel MUST NOT be built against the newest numpy; the wheel-build step targets the declared numpy floor, because numpy's C ABI is forward-compatible only and a wheel built against new numpy segfaults/undefined-symbols on users with older numpy (Pitfall 3)."
      status: flagged
      verification: backstop
---

<objective>
Stand up the greenfield CI/packaging pipeline: build an installable wheel and prove it installs via uv (the thin end-to-end packaging slice), then broaden to the full D-34 platform/arch wheel matrix, a test CI workflow, and a numpy/pandas compatibility matrix (PKG-02).

Purpose: Satisfy PKG-01 (wheels for manylinux/macOS/Windows via maturin), PKG-02 (version-matrix CI), and the local-wheel half of PKG-03 (installs via uv).
Output: Three `.github/workflows/*.yml` files and a locally-verified installable wheel. Because this repo has no git remote yet, the matrix runs themselves are confirmed via a post-push human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-benchmark-release-readiness/04-CONTEXT.md
@.planning/phases/04-benchmark-release-readiness/04-RESEARCH.md
@.planning/phases/04-benchmark-release-readiness/04-PATTERNS.md
@.planning/phases/04-benchmark-release-readiness/04-01-SUMMARY.md
</context>

## Phase Goal (MVP — packaging track)

**As a** Python/data user without a Rust toolchain, **I want to** install pydart as a prebuilt wheel for my platform, **so that** I can `import pydart` and use it without compiling anything.

The thinnest complete slice: build one wheel and install it. Then broaden to every platform and version cell.

## Artifacts this phase produces (Plan 03)

New files: `.github/workflows/wheels.yml`, `.github/workflows/ci.yml`, `.github/workflows/compat-matrix.yml`.
Wheels.yml matrix cells: `ubuntu-latest`/`x86_64-unknown-linux-gnu`/manylinux 2014, `ubuntu-24.04-arm`/`aarch64-unknown-linux-gnu`/manylinux 2014, `macos-13`/`x86_64-apple-darwin`, `macos-14`/`aarch64-apple-darwin`, `windows-latest`/`x86_64-pc-windows-msvc`.
Compat-matrix cells: oldest {py3.11, numpy 2.3.0, pandas 3.0.0}, newest {py3.12, numpy 2.5.1, pandas 3.0.5}.

<tasks>

<task type="tracer">
  <name>Task 1: Preflight cleanup + build and install one wheel + author the D-34 wheel matrix</name>
  <files>.github/workflows/wheels.yml</files>
  <read_first>
    - pyproject.toml (post-Plan-01: `[project].name = "pydart-io"`, `[tool.maturin]` config, requires-python `>=3.11`)
    - .planning/phases/04-benchmark-release-readiness/04-RESEARCH.md (Runtime State Inventory: exact stale-artifact paths to remove; Architecture Patterns Pattern 2: the matrix `include:` block; Pitfall 3: build against numpy floor; Pitfall 5: native arm64 runner)
    - .planning/phases/04-benchmark-release-readiness/04-PATTERNS.md (No Analog Found table — use RESEARCH.md YAML skeletons verbatim)
  </read_first>
  <action>
    First, remove the stale pre-rename build artifacts flagged in RESEARCH.md's Runtime State Inventory so no `flint`-named artifact can contaminate a local/manual build: delete `python/flint`, `target/wheels/flint-*.whl`, `target/debug/*flint*`, `target/release/*flint*`, `target/doc/*flint*` (or run `cargo clean && rm -rf python/flint`). These are untracked; removal has no git effect.
    Build a wheel locally for the host platform: `uv run maturin build --release --out dist`. Then in a throwaway environment prove installability: create a fresh venv, `uv pip install` the built wheel from dist/, and run `python -c "import pydart; print(pydart.__name__)"` to confirm the `pydart-io` distribution installs the importable `pydart` package.
    Create `.github/workflows/wheels.yml`: a `maturin-action@v1`-based build job with a `strategy.matrix.include` covering all five D-34 cells (linux x86_64 + linux aarch64 on `ubuntu-24.04-arm` native runner, macOS x86_64 on `macos-13`, macOS arm64 on `macos-14`, windows x86_64), each with `manylinux: "2014"` for the linux cells, `args: --release --out dist`, and an `actions/upload-artifact@v4` step per cell. Ensure the build targets the numpy floor (Pitfall 3), not the newest numpy. Trigger on `workflow_dispatch` and on tag push (the release workflow in Plan 04 will consume these artifacts).
  </action>
  <verify>
    <automated>test -n "$(ls dist/*.whl 2>/dev/null)" && python3 -c "import zipfile,glob,sys; w=glob.glob('dist/*.whl')[0]; sys.exit(0 if 'pydart/' in ''.join(zipfile.ZipFile(w).namelist()) else 1)" && grep -q 'aarch64-unknown-linux-gnu' .github/workflows/wheels.yml && grep -q 'ubuntu-24.04-arm' .github/workflows/wheels.yml && grep -q 'aarch64-apple-darwin' .github/workflows/wheels.yml && grep -q 'x86_64-apple-darwin' .github/workflows/wheels.yml && grep -q 'windows' .github/workflows/wheels.yml && grep -q 'x86_64-unknown-linux-gnu' .github/workflows/wheels.yml</automated>
  </verify>
  <acceptance_criteria>
    - A `.whl` exists in dist/ and its archive contains the `pydart/` importable package
    - `uv pip install` of that wheel into a fresh venv succeeds and `python -c "import pydart"` exits 0
    - No `python/flint` directory or `flint`-named artifact remains under target/ (grep/ls confirms absence)
    - wheels.yml declares all five D-34 target cells (grep confirms each of the five `target` triples/runner labels)
    - wheels.yml uses `PyO3/maturin-action` and uploads artifacts
  </acceptance_criteria>
  <done>A host-platform wheel builds and installs via uv with a working `import pydart`, and wheels.yml declares the full cross-platform/arch build matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Author the CI test workflow and the numpy/pandas compatibility matrix</name>
  <files>.github/workflows/ci.yml, .github/workflows/compat-matrix.yml</files>
  <read_first>
    - .planning/phases/04-benchmark-release-readiness/04-RESEARCH.md (Recommended Project Structure ci.yml/compat-matrix.yml roles; Open Question 3 endpoint versions; Pitfall 4 per-interpreter resolution)
    - pyproject.toml (dev dependency group; the uv build/test one-liner comment: `uv sync --dev`, `uv run maturin develop`, `uv run pytest`)
    - .planning/STATE.md (the Phase 2 note that gates must run `maturin develop` before pytest, not bare `cargo test`, or Python-visible regressions from Rust changes go undetected)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` triggering on push and pull_request: a job that installs uv, runs `uv sync --dev`, `uv run maturin develop`, `cargo test --workspace`, and `uv run pytest`. Explicitly run `maturin develop` before `pytest` (STATE.md: bare `cargo test` never rebuilds the installed PyO3 extension, so Python-visible regressions slip through).
    Create `.github/workflows/compat-matrix.yml` (PKG-02, D-37) using a two-endpoint `strategy.matrix.include` (not a full cross-product, per RESEARCH.md OQ3 and yolo/coarse settings): oldest = {python-version 3.11, numpy 2.3.0, pandas 3.0.0}; newest = {python-version 3.12, numpy 2.5.1, pandas 3.0.5}. Each cell installs the pinned numpy/pandas, runs `uv run maturin develop`, and runs `uv run pytest`. Note inline that numpy 2.5.x requires Python >=3.12 so it pairs only with the 3.12 cell (Pitfall 4).
  </action>
  <verify>
    <automated>grep -q 'maturin develop' .github/workflows/ci.yml && grep -q 'pytest' .github/workflows/ci.yml && grep -q 'cargo test' .github/workflows/ci.yml && grep -q '3.11' .github/workflows/compat-matrix.yml && grep -q '3.12' .github/workflows/compat-matrix.yml && grep -Eq '2\.3\.0' .github/workflows/compat-matrix.yml && grep -Eq '2\.5\.1' .github/workflows/compat-matrix.yml && grep -Eq '3\.0\.0' .github/workflows/compat-matrix.yml && grep -Eq '3\.0\.5' .github/workflows/compat-matrix.yml</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml runs `uv run maturin develop` BEFORE `uv run pytest` and also runs `cargo test` (grep confirms all three)
    - compat-matrix.yml declares Python 3.11 with numpy 2.3.0 + pandas 3.0.0 (oldest endpoint) and Python 3.12 with numpy 2.5.1 + pandas 3.0.5 (newest endpoint)
    - compat-matrix.yml notes that numpy 2.5.x pairs only with Python 3.12 (Pitfall 4)
  </acceptance_criteria>
  <done>ci.yml and compat-matrix.yml are authored with the correct build-then-test ordering and the D-37 two-endpoint version matrix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the wheel matrix and compat matrix are green on GitHub</name>
  <what-built>Three workflow files (wheels.yml full D-34 matrix, ci.yml, compat-matrix.yml two-endpoint) validated locally (a host wheel was built and installed via uv), but the cross-platform matrix and version matrix cannot run without the repo on GitHub — this repo has no git remote.</what-built>
  <how-to-verify>
    1. Create the GitHub repo (public — see user_setup, required for the free native `ubuntu-24.04-arm` runner) and push. If the repo must be private, first switch the aarch64 Linux job to a QEMU cross-compile fallback (`docker/setup-qemu-action`), since the free native-arm tier may not apply.
    2. Trigger `wheels.yml` (workflow_dispatch). Confirm all five (OS, arch) cells build a wheel and upload an artifact — none skipped or failing (PKG-01).
    3. Confirm `ci.yml` passes on the push (cargo test + maturin develop + pytest).
    4. Confirm `compat-matrix.yml` passes both the oldest and newest endpoint cells (PKG-02).
    5. If `macos-13` has been retired by GitHub (Assumption A2), substitute the current macOS x86_64 runner label and re-run.
  </how-to-verify>
  <resume-signal>Type "approved" once the wheel matrix and compat matrix are green (or describe which cells failed and how they were resolved).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev machine → wheel artifact | A manual/local build could pick up a stale on-disk artifact from a prior project name |
| CI workflow permissions → repo | Workflow token scope determines what a compromised job can do |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Elevation of Privilege | CI workflow `permissions:` | medium | mitigate | ci.yml/wheels.yml/compat-matrix.yml declare no elevated permissions (default read-only); `id-token: write` is scoped to the publish job only, in Plan 04 — never repo/workflow-wide |
| T-04-03 | Tampering | stale flint build artifacts on disk | low | mitigate | Task 1 preflight cleanup removes all `flint`-named artifacts before any local wheel build |
</threat_model>

<verification>
- Local host wheel builds + installs via uv + imports pydart
- wheels.yml declares all five D-34 cells; compat-matrix.yml declares both D-37 endpoints; ci.yml orders maturin-develop before pytest
- Post-push human-verify that the matrices go green (unavoidable — no git remote locally)
</verification>

<success_criteria>
PKG-01, PKG-02, and the local-wheel half of PKG-03 satisfied: an installable wheel proven locally, and the full cross-platform + version-matrix CI authored and (post-push) confirmed green.
</success_criteria>

<output>
Create `.planning/phases/04-benchmark-release-readiness/04-03-SUMMARY.md` when done.
</output>
