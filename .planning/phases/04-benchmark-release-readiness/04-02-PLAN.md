---
phase: 04-benchmark-release-readiness
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - benchmarks/scenarios.py
  - benchmarks/memory/scenarios_memory.py
  - benchmarks/test_bench_from_pandas.py
  - benchmarks/test_bench_to_pandas.py
  - benchmarks/test_bench_parquet_io.py
  - BENCHMARKS.md
autonomous: false
requirements: [BENCH-01, BENCH-02]
user_setup: []

must_haves:
  truths:
    - "The scenario matrix covers all six realistic shapes (numeric_dense, numeric_nullable, mixed_object_string, chunked_multi_batch, categorical_ordered, categorical_unordered) crossed with from_pandas, to_pandas, and Parquet read/write axes (BENCH-01)."
    - "Every matrix cell reports BOTH throughput (pytest-benchmark) and peak RSS (psutil subprocess) — no cell is speed-only (BENCH-02)."
    - "BENCHMARKS.md labels each scenario as 'true zero-copy' or 'copy-fallback' rather than presenting a single blended speedup number (Pitfall 1)."
    - "BENCHMARKS.md states the falsifiable pass bar: true-zero-copy scenarios (numeric_dense, numeric_nullable, chunked_multi_batch) must show pydart throughput >= 2x pyarrow; copy-fallback scenarios (mixed_object_string, categorical_*) must be within +/-20% of pyarrow and reported either way."
    - "BENCHMARKS.md contains the D-40/T-03-09 categorical Parquet fidelity caveat (category order + unused categories do NOT survive a Parquet round-trip; values + dict_is_ordered DO), stated with the specific limitation named, not a vague footnote."
    - statement: "Peak RSS is reported in consistent units (bytes) across Linux/macOS/Windows via psutil normalization (BENCH-02)."
      verification: backstop
  artifacts:
    - "benchmarks/test_bench_to_pandas.py, benchmarks/test_bench_parquet_io.py"
    - "benchmarks/scenarios.py expanded to the full six-scenario SCENARIOS list"
    - "BENCHMARKS.md finalized with the full matrix, zero-copy/copy labels, pass bar, and D-40 caveat"
  key_links:
    - "test_bench_parquet_io.py uses the pytest tmp_path fixture and times table.to_parquet(str(path)) and pydart.Table.from_parquet(str(path)) as two separate cases (D-19 instance/classmethod shape)"
    - "categorical Parquet scenario generation carries an inline comment referencing D-40/T-03-09 at the point of use"
  prohibitions:
    - statement: "Benchmark results MUST NOT be presented as a single blended headline speedup number that hides the per-scenario zero-copy-vs-copy split (Pitfall 1)."
      status: flagged
      verification: human
    - statement: "Peak memory MUST NOT be measured with tracemalloc or any in-process Python-heap profiler — it reports near-zero for the Rust-owned Arrow buffers BENCH-02 must measure (Pitfall 2)."
      status: flagged
      verification: human
    - statement: "BENCHMARKS.md MUST NOT omit the D-40/T-03-09 categorical Parquet fidelity limitation from the categorical scenarios' presentation."
      status: flagged
      verification: human
---

<objective>
Broaden the benchmark harness from the single numeric_dense tracer (Plan 01) to the full realistic scenario matrix across all conversion/IO axes, then finalize BENCHMARKS.md with per-scenario results, honest zero-copy/copy labels, a falsifiable pass bar, and the D-40 categorical Parquet caveat.

Purpose: Satisfy BENCH-01 (realistic matrix) and BENCH-02 (throughput + RSS for every cell) with a claim that survives community scrutiny (Pitfall 1/6).
Output: A finalized BENCHMARKS.md and the complete benchmark suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-benchmark-release-readiness/04-CONTEXT.md
@.planning/phases/04-benchmark-release-readiness/04-RESEARCH.md
@.planning/phases/04-benchmark-release-readiness/04-PATTERNS.md
@.planning/phases/04-benchmark-release-readiness/04-01-SUMMARY.md
</context>

## Phase Goal (MVP — benchmark track)

**As a** prospective pydart adopter, **I want to** see every realistic dtype scenario benchmarked vs pyarrow for both speed and memory, with the honest zero-copy-vs-copy split spelled out, **so that** I can trust the "faster than pyarrow" claim without being misled by a single averaged number.

This plan expands the Plan 01 tracer into the full matrix and turns the skeleton into the shippable claim.

## Artifacts this phase produces (Plan 02)

New files: `benchmarks/test_bench_to_pandas.py`, `benchmarks/test_bench_parquet_io.py`.
New symbols: full `SCENARIOS` list (numeric_dense, numeric_nullable, mixed_object_string, chunked_multi_batch, categorical_ordered, categorical_unordered); per-scenario `make_df` branches; to_pandas and Parquet-IO benchmark cases.
Finalized doc: `BENCHMARKS.md` (full matrix, zero-copy/copy labels, pass bar, D-40 caveat).

## Surfaced assumptions (spec-less probe, not silently dropped)

- BENCH-01 "encoding / definition of length/equality" edge (probe row, unresolved): NOT applicable as a benchmark acceptance criterion. Benchmarks measure throughput and peak RSS, not value-equality or encoding fidelity — round-trip value/encoding correctness is already covered by the Phase 1-3 test suite (test_round_trip.py, test_categorical.py, test_parquet_fidelity.py). Surfaced here as an explicit assumption rather than authored into must_haves.

<tasks>

<task type="auto">
  <name>Task 1: Expand the scenario generators and throughput/RSS matrix</name>
  <files>benchmarks/scenarios.py, benchmarks/memory/scenarios_memory.py, benchmarks/test_bench_from_pandas.py, benchmarks/test_bench_to_pandas.py, benchmarks/test_bench_parquet_io.py</files>
  <read_first>
    - benchmarks/scenarios.py (Plan 01 numeric_dense generator to extend)
    - benchmarks/test_bench_from_pandas.py (Plan 01 parametrization to broaden)
    - benchmarks/memory/scenarios_memory.py (Plan 01 subprocess entry point to broaden)
    - tests/python/test_nulls.py (nullable idiom), tests/python/test_categorical.py (lines 34-48 ordered categorical, lines 82-96 the >255-category int16-code-width fixture), tests/python/test_object_string.py (object/string shapes), tests/python/test_multi_chunk_diagnostics.py (chunked multi-batch shape)
    - .planning/phases/04-benchmark-release-readiness/04-PATTERNS.md (lines 29-63 scenario patterns; the categorical D-40 comment requirement)
  </read_first>
  <action>
    Expand benchmarks/scenarios.py `SCENARIOS` to the full six: numeric_dense, numeric_nullable, mixed_object_string, chunked_multi_batch, categorical_ordered, categorical_unordered. Build numeric_nullable with `pd.array([...with None...], dtype="int64[pyarrow]")`; chunked_multi_batch as a multi-RecordBatch/ChunkedArray-backed frame; categorical_* mirroring test_categorical.py including the large-cardinality (>255-category) int16-code-width case. Keep all true-zero-copy scenarios (numeric_dense, numeric_nullable, chunked_multi_batch) on the `[pyarrow]` ArrowDtype path. In the categorical scenario generators used for Parquet IO, add an inline comment referencing D-40/T-03-09 so the file documents the known non-round-trip-of-category-order limitation at the point of use.
    Broaden benchmarks/test_bench_from_pandas.py to parametrize over all six scenarios (pydart vs pyarrow cases each).
    Create benchmarks/test_bench_to_pandas.py mirroring the from_pandas structure but timing `table.to_pandas()` vs the pyarrow `Table.to_pandas()` equivalent, over all six scenarios.
    Create benchmarks/test_bench_parquet_io.py using the pytest `tmp_path` fixture: two benchmark cases per scenario — one timing `table.to_parquet(str(path))` (write) and one timing `pydart.Table.from_parquet(str(path))` (read), against the pyarrow parquet write/read equivalents. Follow tests/python/test_parquet_roundtrip.py's import and file-IO pattern.
    Broaden benchmarks/memory/scenarios_memory.py to accept any of the six scenario names as its subprocess argument so measure_rss.py (Plan 01) can measure peak RSS per scenario.
  </action>
  <verify>
    <automated>uv run pytest benchmarks/ -q && for s in numeric_dense numeric_nullable mixed_object_string chunked_multi_batch categorical_ordered categorical_unordered; do uv run python benchmarks/memory/measure_rss.py "$s" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - benchmarks/scenarios.py `SCENARIOS` list contains all six scenario names (grep confirms each)
    - `uv run pytest benchmarks/` collects and passes cases for from_pandas, to_pandas, and parquet_io across all six scenarios
    - benchmarks/memory/measure_rss.py returns a peak-RSS integer for each of the six scenarios
    - categorical Parquet scenario generation contains an inline `D-40` / `T-03-09` reference comment (grep confirms)
    - benchmarks/test_bench_parquet_io.py uses `tmp_path` and calls `table.to_parquet` and `pydart.Table.from_parquet` (grep confirms)
  </acceptance_criteria>
  <done>All six scenarios run across from_pandas/to_pandas/parquet-read/parquet-write with both throughput and per-scenario peak RSS.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize BENCHMARKS.md with matrix, honest labels, pass bar, and D-40 caveat</name>
  <files>BENCHMARKS.md</files>
  <read_first>
    - BENCHMARKS.md (Plan 01 skeleton)
    - crates/pydart-python/src/error.rs (lines 1-6: the diagnostics-honesty / name-the-specific-limitation doc convention)
    - .planning/STATE.md (the Phase 3 categorical Parquet fidelity gap entry / T-03-09)
    - .planning/phases/04-benchmark-release-readiness/04-RESEARCH.md (Pitfall 1, Pitfall 6, Open Question 2 pass-bar recommendation)
    - .planning/phases/04-benchmark-release-readiness/04-CONTEXT.md (D-38, D-39, D-40)
  </read_first>
  <action>
    Regenerate BENCHMARKS.md from actual runs of the full suite. Structure: (1) Methodology — synthetic-only data (D-39) with documented shapes/row counts, pytest-benchmark throughput, psutil-subprocess peak RSS in bytes, criterion for Rust kernels, committed-in-repo per D-38 (no external dashboard). (2) A results matrix table with one row per (scenario x axis) cell, each reporting pydart throughput, pyarrow throughput, ratio, pydart peak RSS, pyarrow peak RSS. (3) An explicit per-scenario label column marking "true zero-copy" (numeric_dense, numeric_nullable, chunked_multi_batch) vs "copy-fallback" (mixed_object_string, categorical_*) — never a single blended number (Pitfall 1). (4) A stated, falsifiable pass bar: true-zero-copy scenarios must show pydart throughput >= 2x pyarrow AND pydart peak RSS <= pyarrow peak RSS; copy-fallback scenarios must be within +/-20% of pyarrow on throughput and are reported either way (win or loss). (5) A "Known Limitations" section naming the D-40/T-03-09 categorical Parquet gap specifically: a categorical's `.cat.categories` order and unused categories do NOT survive a Parquet round-trip (arrow-rs ArrowWriter/DictEncoder limitation); per-row values and `dict_is_ordered` DO survive — with the same specificity the project applies to its Rust error variants, not a vague "limitations may apply" note.
  </action>
  <verify>
    <automated>grep -q '2x' BENCHMARKS.md && grep -Eqi 'zero.?copy' BENCHMARKS.md && grep -Eqi 'copy.?fallback' BENCHMARKS.md && grep -q 'T-03-09' BENCHMARKS.md && grep -Eqi 'dict_is_ordered' BENCHMARKS.md && grep -Eqi 'RSS' BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - BENCHMARKS.md contains a results table with a row per scenario x axis, each showing both a throughput comparison and a peak-RSS comparison
    - BENCHMARKS.md labels each scenario "true zero-copy" or "copy-fallback" (grep confirms both terms present)
    - BENCHMARKS.md states the falsifiable pass bar: `>= 2x` for true-zero-copy, `+/-20%` for copy-fallback (grep confirms `2x` and `20%`)
    - BENCHMARKS.md names the D-40 categorical Parquet limitation with the specific `.cat.categories`-order + unused-category wording and cites `T-03-09`
    - No single blended "Nx faster overall" headline appears in place of the per-scenario table
  </acceptance_criteria>
  <done>BENCHMARKS.md is the shippable, scrutiny-surviving claim: per-scenario throughput + RSS, honest zero-copy/copy labels, a falsifiable pass bar, and the named categorical Parquet caveat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sign off benchmark results against the stated pass bar</name>
  <what-built>The full six-scenario x four-axis benchmark matrix with throughput + peak RSS per cell, and a finalized BENCHMARKS.md stating a falsifiable pass bar (true-zero-copy >= 2x pyarrow throughput and RSS <= pyarrow; copy-fallback within +/-20%, reported either way) plus the D-40 categorical Parquet caveat.</what-built>
  <how-to-verify>
    1. Open BENCHMARKS.md and confirm the per-scenario table reports both throughput and peak RSS for every cell.
    2. Check the true-zero-copy scenarios (numeric_dense, numeric_nullable, chunked_multi_batch) against the stated `>= 2x throughput` / `RSS <= pyarrow` bar. Decide whether the project's existential "measurably faster than pyarrow" claim (PROJECT.md) is met, or whether any true-zero-copy cell that misses the bar is a real blocker to sealing this phase.
    3. Confirm the copy-fallback scenarios are reported honestly (win or loss) and within the +/-20% band, or that any deviation is acknowledged.
    4. Confirm the D-40 categorical Parquet caveat is present and specific.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the benchmark claim and pass bar as the phase's proof of the core value, or describe which scenarios must be re-run / re-tuned / re-scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan exercises existing pydart/pyarrow APIs on synthetic in-process data; no external input, no new dependency, no release surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | BENCHMARKS.md public claim | low | mitigate | Honesty prohibitions (no blended number, no tracemalloc-measured memory, no omitted D-40 caveat) enforced via must_haves.prohibitions and the Task 3 human-verify sign-off |
</threat_model>

<verification>
- Full matrix runs (six scenarios x from/to/parquet-read/parquet-write), each with throughput + peak RSS
- BENCHMARKS.md has honest labels, falsifiable pass bar, and named D-40 caveat
- Human sign-off on the claim vs the pass bar
</verification>

<success_criteria>
BENCH-01 and BENCH-02 satisfied by a per-scenario matrix reporting both speed and memory, published as a scrutiny-surviving BENCHMARKS.md with a signed-off pass bar.
</success_criteria>

<output>
Create `.planning/phases/04-benchmark-release-readiness/04-02-SUMMARY.md` when done.
</output>
