---
phase: 01-core-zero-copy-round-trip-interop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - crates/flint-core/Cargo.toml
  - crates/flint-core/src/lib.rs
  - crates/flint-core/src/table.rs
  - crates/flint-python/Cargo.toml
  - crates/flint-python/src/lib.rs
  - crates/flint-python/src/table.rs
  - crates/flint-python/src/error.rs
  - pyproject.toml
  - python/flint/__init__.py
  - tests/python/test_round_trip.py
  - tests/python/test_export_smoke.py
autonomous: true
requirements: [CONV-01, CONV-02, CAP-01]
tags: [rust, pyo3, arrow, maturin, scaffold, walking-skeleton]

must_haves:
  truths:
    - "Running `maturin develop` builds the extension and `python -c 'import flint; flint.Table'` succeeds"
    - "A user can round-trip a non-null int64/float64 ArrowDtype pandas DataFrame: from_pandas then to_pandas returns equal values (D-01, D-02)"
    - "A user can export a Table to pyarrow via __arrow_c_stream__ and pyarrow accepts it without error (CAP-01)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Two-crate workspace root (flint-core + flint-python)"
      contains: "[workspace]"
    - path: "crates/flint-python/src/table.rs"
      provides: "#[pyclass(name = \"Table\")] composing pyo3_arrow::PyTable; from_pandas/to_pandas; PyCapsule export dunders; column + buffer_address accessors"
      contains: "pyclass"
    - path: "pyproject.toml"
      provides: "maturin build backend, uv-compatible dev config"
      contains: "maturin"
    - path: "python/flint/__init__.py"
      provides: "re-exports Table"
      contains: "Table"
    - path: "tests/python/test_round_trip.py"
      provides: "end-to-end numeric round-trip test (skeleton happy path)"
  key_links:
    - from: "crates/flint-python/src/table.rs"
      to: "pyo3_arrow::PyTable"
      via: "Table composes PyTable as inner field and delegates __arrow_c_stream__/__arrow_c_schema__"
      pattern: "PyTable"
    - from: "python/flint/__init__.py"
      to: "crates/flint-python/src/lib.rs"
      via: "#[pymodule] exposes Table, re-exported by the Python package"
      pattern: "Table"
---

<objective>
Stand up the Walking Skeleton for Flint: the thinnest end-to-end stack that proves the whole pipeline works — a two-crate Rust workspace (flint-core + flint-python), a `Table` `#[pyclass]` (D-01) that composes `pyo3_arrow::PyTable`, and a thin `from_pandas`/`to_pandas` (D-02) numeric happy path that round-trips a non-null int64/float64 `ArrowDtype` DataFrame and exports to pyarrow via the Arrow PyCapsule Interface (CAP-01).

Purpose: Establish the foundational architecture (crate layout, Table composition, module wiring, uv/maturin dev workflow) that every later Phase 1 slice and every future phase builds on. This is the Walking Skeleton — it is intentionally the narrowest capability that touches Rust core, PyO3 binding, the pandas boundary, and one real external-consumer PyCapsule handoff.

Output: A buildable, importable `flint` extension; a passing end-to-end round-trip test; a passing pyarrow export smoke test; and SKELETON.md already recorded in this phase directory.

Covers CONV-01 (numeric happy path only — full decision matrix + bool is Plan 02; zero-copy proof is Plan 03), CONV-02 (numeric round-trip correctness), CAP-01 (export to pyarrow). Honors D-01, D-02.
</objective>

## Phase Goal

**As a** pyarrow-familiar Python developer, **I want to** round-trip a numeric/bool pandas DataFrame through Arrow zero-copy with strict-mode diagnostics and PyCapsule interop, **so that** I can adopt a faster, leaner pandas↔Arrow bridge without changing my mental model.

> Derivation note: the ROADMAP.md `**Goal:**` line for Phase 1 is written as capability prose, not the canonical "As a / I want to / so that" form. Per the user-story-template rules this story is derived faithfully from that prose (actor = the pyarrow-familiar Python/data user named in PROJECT.md/CONTEXT.md); it is intentionally broad because the orchestrator scoped all six Phase 1 requirements into this one MVP phase. No new scope was invented.

<artifacts_this_phase_produces>
Symbols created across Phase 1 (all four plans) — listed here so drift/source-grounding review excludes newly-created symbols:

**Python (package `flint`):** `flint.Table` class; `Table.from_pandas(df, strict=False)` classmethod; `Table.to_pandas(strict=False)`; `Table.__arrow_c_stream__`; `Table.__arrow_c_schema__`; `Table.__arrow_c_array__`; `Table.column(name)`; `Table.buffer_address(index)` (buffer-address accessor for pointer-identity proof); `Table.copy_report()`; `flint.from_arrow(obj)`; `flint.FlintError`; `flint.ZeroCopyRequiredError`; `flint.ColumnCopyStatus` (frozen dataclass: `column`, `dtype`, `zero_copy`, `reason`).

**Rust (crate `flint-core`):** `Table`/`RecordBatch` re-export or thin wrapper; `plan_column(...)` fn; `ColumnPlan` enum (`ZeroCopyBorrow`, `RequiresCopy { reason }`); `DtypeBackend` enum (`Arrow`, `Numpy`); `ArrowKind` enum (`Numeric`, `Bool`); `from_numpy_buffer(...)` conversion entry used by the allocation-counter proof.

**Rust (crate `flint-python`):** `#[pyclass(name = "Table")]` composing `pyo3_arrow::PyTable`; `#[pyfunction] from_arrow`; `FlintError` Rust error enum + single `impl From<FlintError> for PyErr`.

**Files/config:** `Cargo.toml` (workspace), `rust-toolchain.toml`, `crates/flint-core/*`, `crates/flint-python/*`, `pyproject.toml`, `python/flint/__init__.py`, `tests/python/*`, `tests/rust/zero_copy_alloc.rs`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-core-zero-copy-round-trip-interop/01-CONTEXT.md
@.planning/phases/01-core-zero-copy-round-trip-interop/01-RESEARCH.md
@.planning/phases/01-core-zero-copy-round-trip-interop/01-PATTERNS.md
@.planning/phases/01-core-zero-copy-round-trip-interop/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install toolchain, scaffold the two-crate workspace, and land a failing end-to-end round-trip test</name>
  <files>Cargo.toml, rust-toolchain.toml, crates/flint-core/Cargo.toml, crates/flint-core/src/lib.rs, crates/flint-core/src/table.rs, crates/flint-python/Cargo.toml, crates/flint-python/src/lib.rs, crates/flint-python/src/table.rs, crates/flint-python/src/error.rs, pyproject.toml, python/flint/__init__.py, tests/python/test_round_trip.py</files>
  <read_first>
    - .planning/phases/01-core-zero-copy-round-trip-interop/SKELETON.md (Architectural Decisions table — crate layout, Table composition, build/dev workflow are locked here)
    - .planning/phases/01-core-zero-copy-round-trip-interop/01-RESEARCH.md lines 127-152 (Recommended Project Structure), lines 401-416 (Environment Availability — Rust toolchain + maturin NOT installed), lines 269-294 (Table composing PyTable)
    - .planning/phases/01-core-zero-copy-round-trip-interop/01-PATTERNS.md lines 51-93 (table.rs pattern), lines 220-250 (shared error-boundary + GIL patterns)
    - .claude/CLAUDE.md (Technology Stack: pinned versions — pyo3 0.29.0, pyo3-arrow 0.19.0, arrow 59.1.0, maturin 1.14.1; abi3 wheel guidance; "What NOT to Use")
  </read_first>
  <action>
    Wave-0 prerequisite (RESEARCH.md Environment Availability): neither the Rust toolchain nor maturin is installed. First install a stable Rust toolchain via rustup (`curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y`, then source the cargo env), or the distro package if rustup is unavailable; pin the toolchain with a `rust-toolchain.toml` declaring channel `stable` (MSRV floor ~1.75 per CLAUDE.md). Then add dev tooling and Python dev dependencies via uv per RESEARCH.md line 98-101: `uv add --dev maturin pandas pyarrow polars duckdb numpy pytest hypothesis` and add `allocation-counter` as a Rust dev-dependency of `flint-core` (gated `#[cfg(test)]`) now so Plan 03 needs no Cargo.toml edit. Pin the versions RESEARCH.md verified (pandas 3.0.3, pyarrow 25.0.0, polars 1.42.1, duckdb 1.5.4, numpy 2.5.1, pytest 9.1.1, hypothesis 6.156.6); all were cleared "Approved" in the RESEARCH.md Package Legitimacy Audit with primary-source `pip index versions` evidence, so install directly (no human-verify gate).

    Scaffold the two-crate workspace exactly as SKELETON.md's Architectural Decisions lock it: root `Cargo.toml` with `[workspace]` members `crates/flint-core` and `crates/flint-python`. `flint-core` is pure Rust depending only on `arrow` 59.1.0 (no pyo3); for Phase 1 it may re-export arrow-rs `RecordBatch`/`Schema` types or wrap them thinly in `table.rs`, and must expose a `from_numpy_buffer`-style entry point stub that Plan 03's allocation-counter test will target. `flint-python` depends on `pyo3` 0.29.0 (features `extension-module`, `abi3-py39`), `pyo3-arrow` 0.19.0, `arrow` 59.1.0, `thiserror`, and `flint-core`.

    In `crates/flint-python/src/table.rs` define `#[pyclass(name = "Table")]` composing a `pyo3_arrow::PyTable` as an internal field (RESEARCH.md Pattern 1 / D-01) — do NOT hand-roll FFI struct construction (CLAUDE.md "What NOT to Use"). Add `#[pymethods]` stubs for `from_pandas` (classmethod, signature `from_pandas(df, strict=False)`) and `to_pandas(strict=False)` that currently raise `NotImplementedError` (so the round-trip test fails RED), plus a `column(name)` accessor and a `buffer_address(index)` accessor returning the integer address of a column's data buffer (used by Plan 03's pointer-identity proof). In `crates/flint-python/src/error.rs` define a `FlintError` enum with `thiserror` and a single `impl From<FlintError> for PyErr` (RESEARCH.md "Don't Hand-Roll" / PATTERNS.md shared error boundary) — the centralized boundary that later makes D-03's column-naming exception consistent. `crates/flint-python/src/lib.rs` declares `#[pymodule]` (module name `flint` or `_flint` per the pyproject module path) registering `Table`. `python/flint/__init__.py` re-exports `Table`.

    Configure `pyproject.toml` with `maturin` as the PEP 517 build backend and the correct module path (matching the `#[pymodule]` name), uv-compatible. Document the local full-stack run command (`maturin develop && uv run pytest`) in the pyproject or a short README stanza.

    Write `tests/python/test_round_trip.py` with a failing (RED) end-to-end test: build `pd.DataFrame({"a": pd.array([1,2,3], dtype="int64[pyarrow]"), "b": pd.array([1.5,2.5], dtype="float64[pyarrow]")})` (use consistent lengths), call `flint.Table.from_pandas(df)` then `.to_pandas()`, and assert frame equality. It must currently fail because `from_pandas`/`to_pandas` raise NotImplementedError — confirming the test exercises the real path before Task 2 implements it.

    Do NOT implement conversion logic in this task — the round-trip test must be RED at the end of Task 1.
  </action>
  <behavior>
    - `rustc --version` and `maturin --version` both succeed after toolchain install.
    - `cargo build` compiles both crates; `maturin develop` produces an importable `flint`.
    - `python -c "import flint; flint.Table"` succeeds (class exists).
    - `uv run pytest tests/python/test_round_trip.py` FAILS with NotImplementedError (RED state confirmed).
  </behavior>
  <verify>
    <automated>rustc --version && maturin --version && cargo build && maturin develop && python -c "import flint; assert hasattr(flint, 'Table')" && ! uv run pytest tests/python/test_round_trip.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Root `Cargo.toml` contains `[workspace]` with members `crates/flint-core` and `crates/flint-python`.
    - `crates/flint-core/Cargo.toml` has NO `pyo3`/`pyo3-arrow` dependency (pure Rust core) and lists `allocation-counter` under dev-dependencies.
    - `crates/flint-python/src/table.rs` contains `#[pyclass(name = "Table")]` and an inner `pyo3_arrow::PyTable` field; no manual `FFI_ArrowArray`/`FFI_ArrowSchema` construction appears (delegation only).
    - `python -c "import flint; flint.Table"` exits 0 after `maturin develop`.
    - `uv run pytest tests/python/test_round_trip.py` exits non-zero (RED) because `from_pandas`/`to_pandas` raise NotImplementedError.
  </acceptance_criteria>
  <done>Toolchain + maturin + dev deps installed; two-crate workspace scaffolded; `Table` pyclass composes `pyo3_arrow::PyTable` and imports cleanly; end-to-end round-trip test is written and RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the thin numeric round-trip and one pyarrow PyCapsule export (turn the skeleton GREEN)</name>
  <files>crates/flint-core/src/table.rs, crates/flint-python/src/table.rs, python/flint/__init__.py, tests/python/test_round_trip.py, tests/python/test_export_smoke.py</files>
  <read_first>
    - crates/flint-python/src/table.rs (current state from Task 1 — the stubs to fill)
    - .planning/phases/01-core-zero-copy-round-trip-interop/01-RESEARCH.md lines 154-162 (Pattern 1 export + Pattern 2 import), lines 309-322 (D-06a buffer accessor sketch), lines 164-189 (Pattern 3 — per-column detection; Phase 1 numeric ArrowDtype path)
    - .planning/phases/01-core-zero-copy-round-trip-interop/01-PATTERNS.md lines 51-93 (compose-don't-reimplement)
    - .claude/CLAUDE.md (Critical Caveat: only ArrowDtype/pyarrow-backed columns are genuinely zero-copy; numeric numpy contiguous can be borrowed)
  </read_first>
  <action>
    Implement the thinnest real `from_pandas`/`to_pandas` (D-02) for the numeric happy path only: non-null `int64[pyarrow]`/`float64[pyarrow]` (`ArrowDtype`-backed) columns. `from_pandas` reads each column's underlying Arrow array (already Arrow's own memory) and builds the composed `pyo3_arrow::PyTable` without copying the data buffer; wrap it in `Table` (D-01). `to_pandas` reconstructs a pandas DataFrame whose columns are `ArrowDtype`-backed and share the Table's Arrow buffers. Keep it narrow — the full per-column decision matrix, numpy-backed borrow, and bool handling are Plan 02; do not add them here. If a column is anything other than a supported numeric `ArrowDtype`, raise a clear error naming the column (a placeholder for the Plan 02 strict/diagnostics surface — do NOT silently copy).

    Delegate the PyCapsule export dunders on `Table` to the composed inner `PyTable` (RESEARCH.md Pattern 1): `__arrow_c_stream__(self, requested_schema=None)` and `__arrow_c_schema__(self)` forward directly to `pyo3_arrow::PyTable`'s implementations (confirm the exact pyo3-arrow 0.19.0 method names against docs.rs before wiring — RESEARCH.md Assumption A3). Implement `buffer_address(index)` to return the integer address of a column's data buffer via the arrow-rs buffer API (this backs Plan 03's pointer-identity proof; D-06). Re-export nothing new from `__init__.py` beyond `Table` in this plan.

    Update `tests/python/test_round_trip.py` so it now PASSES (GREEN): the int64/float64 `ArrowDtype` DataFrame round-trips with equal values and dtypes preserved. Add `tests/python/test_export_smoke.py` proving CAP-01 export against one real consumer: construct a `Table`, pass it to `pyarrow.table(...)` (which consumes `__arrow_c_stream__`), and assert the resulting pyarrow Table has the expected schema and row count with no exception. This is the skeleton's single real external-consumer PyCapsule handoff; full 3-library validation (Polars, DuckDB) is Plan 04.
  </action>
  <behavior>
    - `Table.from_pandas(df)` on an int64/float64 ArrowDtype DataFrame returns a Table; `.to_pandas()` returns a DataFrame equal in values and dtype to the input.
    - `pyarrow.table(flint_table)` returns a pyarrow.Table with matching schema and row count (CAP-01 export path works via __arrow_c_stream__).
    - `Table.buffer_address(0)` returns a nonzero integer address.
    - Passing an unsupported column dtype raises a column-naming error (no silent copy).
  </behavior>
  <verify>
    <automated>maturin develop && uv run pytest tests/python/test_round_trip.py tests/python/test_export_smoke.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/python/test_round_trip.py` now exits 0 (GREEN) — int64/float64 `ArrowDtype` round-trip preserves values and dtypes (CONV-01 numeric happy path, CONV-02 correctness).
    - `tests/python/test_export_smoke.py` passes: `pyarrow.table(<flint Table>)` succeeds and the pyarrow Table's `num_rows`/schema match the input (CAP-01).
    - `Table.__arrow_c_stream__` and `Table.__arrow_c_schema__` bodies forward to the composed `pyo3_arrow::PyTable` (no hand-rolled FFI).
    - `Table.buffer_address(0)` returns a nonzero int on a populated Table.
    - Passing a non-`ArrowDtype`/unsupported column to `from_pandas` raises an error whose message contains the offending column name (no silent copy).
  </acceptance_criteria>
  <done>The Walking Skeleton is GREEN end-to-end: numeric ArrowDtype round-trip works (CONV-01/CONV-02 happy path), one real pyarrow PyCapsule export succeeds (CAP-01), and `buffer_address` is available for the Plan 03 proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registries (PyPI / crates.io) → dev environment | Toolchain, maturin, and dev/test dependencies are downloaded and executed during scaffold/build |
| pandas/numpy memory → Rust core (via pyo3-arrow) | Data buffers cross the FFI boundary during `from_pandas`/`to_pandas` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm/uv/cargo installs (maturin, pandas, pyarrow, polars, duckdb, numpy, pytest, hypothesis, allocation-counter) | high | mitigate | RESEARCH.md Package Legitimacy Audit already cleared every package to "Approved" with primary-source `pip index versions`/crates.io evidence (documented tool false-positive on `too-new`); install the RESEARCH.md-pinned versions only. No blocking human-verify checkpoint required per the researcher's explicit primary-source clearance. All Python deps are dev/test-only — none becomes a runtime dependency of the shipped `flint` wheel. |
| T-01-01 | Tampering | `Table.from_pandas` composing pyo3_arrow::PyTable | medium | mitigate | Compose `pyo3_arrow::PyTable` and delegate FFI marshalling to it (Pattern 1); do NOT add an unchecked `unsafe` shortcut around pyo3-arrow's safe path. Skeleton path handles only `ArrowDtype`-backed numeric columns (already Arrow's own memory), minimizing raw-buffer handling this plan. |
| T-01-02 | Denial of Service | `maturin develop` build step | low | accept | Build failures surface as non-zero exit in CI/local; no untrusted external input drives the build beyond the audited dependency set (T-01-SC). |
</threat_model>

<verification>
- `cargo build` compiles both workspace crates with no errors.
- `maturin develop` builds and installs the extension into the uv-managed environment.
- `python -c "import flint; flint.Table"` succeeds.
- `uv run pytest tests/python/test_round_trip.py tests/python/test_export_smoke.py` passes (GREEN).
- SKELETON.md exists in the phase directory and its Architectural Decisions match the scaffolded layout (two-crate workspace, Table composes PyTable).
</verification>

<success_criteria>
- A user can `import flint`, call `Table.from_pandas(df).to_pandas()` on a non-null numeric `ArrowDtype` DataFrame and get equal values back (CONV-01 numeric happy path, CONV-02 correctness).
- A user can hand a `Table` to `pyarrow.table(...)` via the PyCapsule Interface and pyarrow accepts it (CAP-01).
- The two-crate workspace, `Table` composition, and uv/maturin dev workflow match the SKELETON.md contract for later plans to build on.
</success_criteria>

<output>
Create `.planning/phases/01-core-zero-copy-round-trip-interop/01-01-SUMMARY.md` when done.
</output>
