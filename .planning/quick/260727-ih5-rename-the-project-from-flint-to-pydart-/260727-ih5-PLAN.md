---
phase: quick-260727-ih5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/flint-core/**
  - crates/flint-python/**
  - tests/rust/*.rs
  - pyproject.toml
  - python/flint/**
  - tests/python/*.py
  - .gitignore
  - .claude/CLAUDE.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RENAME-01]

must_haves:
  truths:
    - "`import pydart` works and `import flint` no longer resolves"
    - "`uv run maturin develop && uv run pytest` passes with zero collection or import errors"
    - "The compiled extension registers as `pydart._pydart` and exposes `pydart.PydartError`"
    - "The Rust workspace builds and the flint-core integration tests compile under the new crate name"
  artifacts:
    - crates/pydart-core/Cargo.toml
    - crates/pydart-python/Cargo.toml
    - python/pydart/__init__.py
    - pyproject.toml
  key_links:
    - "pyproject.toml `module-name = pydart._pydart` must match `#[pymodule] fn _pydart` in crates/pydart-python/src/lib.rs"
    - "`[lib] name = pydart_core` must match every `use pydart_core::` / `pydart_core::` reference in crates/pydart-python/src and tests/rust/*.rs"
    - "python/pydart/__init__.py `from ._pydart import ...` must match the compiled extension module name and its exported symbols"
---

<objective>
Rename the project from "flint" to "pydart" across the entire codebase — crate names, Rust library names, the compiled extension module, the Python package/import path, the Python-visible exception name, tests, and documentation — in preparation for Phase 4 (Benchmark & Release Readiness) publishing to real PyPI.

Purpose: The project name is currently a placeholder ("flint"); it must be finalized as "pydart" before packaging/publishing work begins. A single missed reference breaks the build or a test-collection step, so the rename is sequenced Rust -> Python -> docs -> full-stack verification with a compile gate after each risky step.

Output: A fully renamed, building, and passing codebase. `import pydart` works, `import flint` is gone, `uv run maturin develop && uv run pytest` is green.

Scope discipline: This is MECHANICAL ONLY — identifier and string renames. No logic changes, no behavior changes, no refactoring. Do NOT rename `ZeroCopyRequiredError` / `PyZeroCopyRequiredError` (they are not Flint-prefixed) — only the `flint.` namespace becomes `pydart.` and `FlintError` becomes `PydartError`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

# Current-state inventory confirmed by grep during planning:
# - Rust crates: crates/flint-core (lib name `flint_core`), crates/flint-python (lib name `_flint`, cdylib)
# - Python pkg: python/flint/ (only __init__.py is git-tracked; *.so is gitignored/untracked)
# - Python-visible symbols: flint.Table, flint.FlintError, flint.ZeroCopyRequiredError, flint.from_arrow, flint.ColumnCopyStatus
# - Rust identifiers: FlintError enum (error.rs), PyFlintError (diagnostics.rs), m.add("FlintError") (lib.rs)
# - Rust integration tests live at repo-root tests/rust/*.rs and use `flint_core::` (referenced from crates/flint-core/Cargo.toml [[test]] path = "../../tests/rust/...")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Rust workspace, crates, lib names, extension module, and error identifiers</name>
  <files>Cargo.toml, crates/flint-core/ (-> crates/pydart-core/), crates/flint-python/ (-> crates/pydart-python/), tests/rust/zero_copy_alloc.rs, tests/rust/concat_generic_arrays.rs, tests/rust/parquet_dictionary_tz_roundtrip.rs, tests/rust/parquet_row_group_pruning.rs</files>
  <action>
Rename the two crate directories with `git mv` to preserve history: `git mv crates/flint-core crates/pydart-core` and `git mv crates/flint-python crates/pydart-python`.

Then update identifiers (mechanical string replacement only):

Root `Cargo.toml`: update `[workspace] members` entries `crates/flint-core` -> `crates/pydart-core` and `crates/flint-python` -> `crates/pydart-python`.

`crates/pydart-core/Cargo.toml`: `[package] name = "flint-core"` -> `"pydart-core"`; `[lib] name = "flint_core"` -> `"pydart_core"`. Also update the stale comment referencing `cargo test -p flint-core --test` -> `-p pydart-core --test`. Do NOT change the `[[test]] path = "../../tests/rust/..."` values — `../../` still resolves to the repo root after the directory move.

`crates/pydart-python/Cargo.toml`: `[package] name = "flint-python"` -> `"pydart-python"`; `[lib] name = "_flint"` -> `"_pydart"`; the path dependency `flint-core = { path = "../flint-core" }` -> `pydart-core = { path = "../pydart-core" }`.

`crates/pydart-python/src/lib.rs`: rename `#[pymodule] fn _flint(...)` -> `fn _pydart(...)`; update the registered exception string `m.add("FlintError", ...)` -> `m.add("PydartError", ...)` (leave the `ZeroCopyRequiredError` registration string unchanged); update the `use diagnostics::{PyFlintError, ...}` import to `PyPydartError`; update doc comments referencing `_flint` / the `flint` package.

Rename the internal Rust error type `FlintError` -> `PydartError` in `crates/pydart-python/src/error.rs` (the enum, `impl From<FlintError> for PyErr`, all variant match arms) and every reference to it in `table.rs`, `import.rs`, `pandas.rs`, `diagnostics.rs`. Rename the Python-exception wrapper `PyFlintError` -> `PyPydartError` in `diagnostics.rs` and its importers (`error.rs`, `import.rs`, `lib.rs`).

Replace every `flint_core::` and `use flint_core` reference with `pydart_core::` / `use pydart_core` across `crates/pydart-python/src/*.rs` AND all four `tests/rust/*.rs` files.

Update remaining Rust doc-comment prose that names "flint"/"Flint"/"flint-core" (e.g. `crates/pydart-core/src/lib.rs` line 1, comment references to `flint.FlintError` become `pydart.PydartError`).

Grep `crates/ tests/rust/` case-sensitively for `Flint` and `flint` afterward and reconcile any remaining hits before verifying.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5 && cargo test -p pydart-core --no-run 2>&1 | tail -5</automated>
  </verify>
  <done>`cargo build --workspace` succeeds and `cargo test -p pydart-core --no-run` compiles all four tests/rust integration tests under the renamed `pydart_core` lib. No `flint`/`Flint` identifiers remain in crates/ or tests/rust/ except intentional none.</done>
</task>

<task type="auto">
  <name>Task 2: Rename Python package, extension binding, tests, and packaging config</name>
  <files>pyproject.toml, python/flint/ (-> python/pydart/), python/pydart/__init__.py, tests/python/*.py (17 files), .gitignore</files>
  <action>
`pyproject.toml`: `[project] name = "flint"` -> `"pydart"`; `[tool.maturin] manifest-path = "crates/flint-python/Cargo.toml"` -> `"crates/pydart-python/Cargo.toml"`; `module-name = "flint._flint"` -> `"pydart._pydart"`. Also update prose comments that name the package or a crate path: the runtime-dependencies comment ("flint must stay leaner than pyarrow" -> "pydart must stay leaner than pyarrow") and the requires-python comment referencing `crates/flint-python` -> `crates/pydart-python` (leave the abi3 version number as-is — version is out of scope).

Rename the package directory with `git mv python/flint python/pydart` (this moves the tracked `__init__.py`). Then remove any leftover untracked build artifacts from the old path so the tree is clean: delete the stale `python/pydart/_flint.abi3.so` if the move carried it, and `rm -rf python/flint` if any untracked files (the old `.abi3.so`, `__pycache__`) remain there. The fresh `_pydart` extension is rebuilt by maturin in Task 4.

`python/pydart/__init__.py`: change `from ._flint import FlintError, Table, ZeroCopyRequiredError, from_arrow` -> `from ._pydart import PydartError, Table, ZeroCopyRequiredError, from_arrow`; in `__all__` replace `"FlintError"` -> `"PydartError"`; update the module docstring prose ("Flint:" title, `_flint` reference, `FlintError` mention) to pydart/`_pydart`/`PydartError`. Leave `ColumnCopyStatus`, `ZeroCopyRequiredError`, `Table`, `from_arrow` symbol names unchanged.

Across all 17 `tests/python/*.py` files: `import flint` -> `import pydart`; every `flint.` attribute access -> `pydart.`; every `flint.FlintError` -> `pydart.PydartError` and any bare `FlintError` reference in `pytest.raises(...)` / assertions -> `PydartError`; update docstring/comment prose naming the package or `flint.FlintError`. Leave `ZeroCopyRequiredError` symbol name unchanged (only its `flint.` -> `pydart.` namespace prefix changes).

`.gitignore`: update the maturin-artifact ignore lines `python/flint/*.so`, `python/flint/*.pyd`, `python/flint/*.dylib` -> `python/pydart/*.so`, `python/pydart/*.pyd`, `python/pydart/*.dylib`.

Grep `python/ tests/python/ pyproject.toml .gitignore` for `flint`/`Flint` afterward and reconcile any remaining hits.
  </action>
  <verify>
    <automated>test -f python/pydart/__init__.py && ! test -d python/flint && grep -q '"pydart"' pyproject.toml && grep -q 'pydart._pydart' pyproject.toml && grep -q 'from ._pydart import' python/pydart/__init__.py && ! grep -rn 'import flint$' tests/python/*.py</automated>
  </verify>
  <done>python/pydart/__init__.py imports from `._pydart` and exports `PydartError`; python/flint/ no longer exists; pyproject.toml declares name `pydart` and module-name `pydart._pydart`; all 17 test files import `pydart`; .gitignore points at python/pydart/. No `flint`/`Flint` identifiers remain in python/ or tests/python/.</done>
</task>

<task type="auto">
  <name>Task 3: Rename forward-facing documentation references</name>
  <files>.claude/CLAUDE.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Update only FORWARD-FACING project-name references; preserve historical narrative that accurately describes what the code was called in past phases.

`.claude/CLAUDE.md` line 5: `**Flint (placeholder name)**` -> `**pydart**` (drop the "placeholder name" framing — the name is now final). This is the only literal `flint`/`Flint` in the file.

`.planning/PROJECT.md`: line 1 heading `# Flint (placeholder name)` -> `# pydart`. Check for any other prose "Flint"/"flint" references in the "What This Is"/title area and update them to pydart. (Grep confirmed 1 occurrence total.)

`.planning/ROADMAP.md`: title line `# Roadmap: Flint (placeholder name)` -> `# Roadmap: pydart`; the narrative intro line "Flint proves its reason to exist..." -> "pydart proves its reason to exist...". Leave the two historical plan-checklist/goal references ("Flint-owned D-11 content validation", "against Flint's Arrow core") as-is — they are the accurate historical record of completed Phase 2/3 work.

`.planning/REQUIREMENTS.md`: title line `# Requirements: Flint (placeholder name)` -> `# Requirements: pydart`.

Do NOT edit `.planning/STATE.md`: its `flint`/`Flint` occurrences are all historical decision-log and blocker entries describing past phases and then-current file paths — the task instruction explicitly says to preserve the historical record. Verify (grep) that STATE.md has no forward-facing project-name title line before concluding no edit is needed; if a title line exists, update only that.
  </action>
  <verify>
    <automated>grep -q '^\*\*pydart\*\*' .claude/CLAUDE.md && grep -q '^# pydart' .planning/PROJECT.md && grep -q '^# Roadmap: pydart' .planning/ROADMAP.md && grep -q '^# Requirements: pydart' .planning/REQUIREMENTS.md && ! grep -q 'placeholder name' .claude/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md, PROJECT.md, ROADMAP.md, and REQUIREMENTS.md carry the finalized "pydart" name with the "placeholder name" framing dropped; historical decision-log entries in ROADMAP.md and STATE.md are left intact as accurate history.</done>
</task>

<task type="auto">
  <name>Task 4: Full build + test verification and residual-reference review</name>
  <files>uv.lock, Cargo.lock (regenerated), no manual source edits</files>
  <action>
This is the certifying gate for the entire rename. Regenerate lockfiles for the renamed packages and run the full build+test cycle end to end:

1. `uv lock` to re-resolve the project under its new name `pydart` (uv.lock currently pins `name = "flint"`). Cargo.lock is regenerated automatically by the cargo/maturin build.
2. `uv run maturin develop` — compiles the renamed `_pydart` extension and installs it into the uv venv as `pydart._pydart`.
3. `uv run pytest` — runs the full Python suite; every test file must import `pydart` and collect cleanly.
4. `cargo test -p pydart-core --no-run` — reconfirms the Rust integration tests compile under the renamed lib (belt-and-suspenders alongside the Python gate; scoped to the pyo3-free core crate to avoid spurious libpython link failures from the extension-module feature).

Then run a residual-reference REVIEW (not a hard zero-count gate): grep `crates/ python/ tests/ pyproject.toml Cargo.toml .gitignore` case-insensitively for `flint`. Any hit here is a genuine miss and must be fixed and re-verified. Do NOT grep-gate `.planning/STATE.md`, `.planning/ROADMAP.md` historical entries, `target/`, `.venv/`, or lockfiles — those legitimately retain historical or regenerated references.

If any step fails, fix the specific missed reference it exposes and re-run from step 2.
  </action>
  <verify>
    <automated>uv lock && uv run maturin develop && uv run pytest && cargo test -p pydart-core --no-run && python -c "import pydart; pydart.PydartError; print('pydart OK')"</automated>
  </verify>
  <done>`uv run maturin develop && uv run pytest` passes with zero import/collection errors; `import pydart` and `pydart.PydartError` resolve; `import flint` no longer works; `cargo test -p pydart-core --no-run` compiles; the scoped residual grep over code+config surfaces no unintended `flint`/`Flint` references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries are introduced. This is a mechanical identifier/string rename with no logic, dependency, or behavior changes, no new package installs, and no new data flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ih5-01 | Tampering | Build integrity (missed reference breaks build/tests) | medium | mitigate | Compile gate (`cargo build --workspace` + `cargo test -p pydart-core --no-run`) after the Rust step and a full `uv run maturin develop && uv run pytest` gate in Task 4; scoped residual grep over code+config. |
</threat_model>

<verification>
- Rust: `cargo build --workspace` and `cargo test -p pydart-core --no-run` both succeed under the renamed crates.
- Python: `uv run maturin develop && uv run pytest` passes with zero import/collection errors.
- Identity: `python -c "import pydart; pydart.PydartError"` works; `import flint` fails.
- Residual: scoped grep over `crates/ python/ tests/ pyproject.toml Cargo.toml .gitignore` shows no unintended `flint`/`Flint` references (historical `.planning/` entries excluded by design).
</verification>

<success_criteria>
- Project builds and the full test suite passes after the rename.
- `pydart` is the package/import name; `pydart._pydart` is the extension module; `pydart.PydartError` is the exception.
- Rust crates are `pydart-core` (lib `pydart_core`) and `pydart-python` (lib `_pydart`).
- Forward-facing docs say "pydart" with the placeholder framing removed; historical decision-log entries preserved.
- No logic or behavior changed — rename only.
</success_criteria>

<output>
This is a quick-mode single-plan task. Record completion in `.planning/STATE.md` Quick Tasks Completed table and commit per the quick workflow. No SUMMARY.md file required.
</output>
